import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Primary
  static const Color primary = Color(0xFFFF4B3A);
  static const Color primaryLight = Color(0xFFFF6B4A);
  static const Color primaryDark = Color(0xFFE53E2E);

  // Background
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color scaffoldBg = Color(0xFFF5F5F5);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF888888);
  static const Color textHint = Color(0xFFBBBBBB);

  // UI Elements
  static const Color divider = Color(0xFFE0E0E0);
  static const Color chipBg = Color(0xFFF0F0F0);
  static const Color chipText = Color(0xFF555555);
  static const Color cardShadow = Color(0x1A000000);

  // States
  static const Color error = Color(0xFFE53935);
  static const Color success = Color(0xFF43A047);

  // Match overlay
  static const Color matchOverlayStart = Color(0xCC1A0A2E);
  static const Color matchOverlayEnd = Color(0xCC3D1A0A);

  // Bottom nav
  static const Color navInactive = Color(0xFF999999);
  static const Color navActive = Color(0xFFFF4B3A);
}

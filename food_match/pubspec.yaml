name: food_match
description: FoodMatch mobile MVP.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.8.1
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.5.4
  json_annotation: ^4.11.0
  http: ^1.2.0
  provider: ^6.1.0
  cached_network_image: ^3.3.0
  flutter_card_swiper: ^7.0.0
  image_picker: ^1.0.0
  shimmer: ^3.0.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
  mockito: ^5.4.0

flutter:
  uses-material-design: true
  assets:
    - assets/media/

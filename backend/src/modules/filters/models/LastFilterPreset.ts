import { Document, Schema, Types, model } from 'mongoose';
export interface LastFilterPresetDocument extends Document { mode:'solo'|'paired'; userId?:Types.ObjectId|null; pairKey?:string|null; dishRegisters:string[]; cuisines:string[]; moods:string[]; diet:string[]; exclusions:string[]; matchedLastTime:number; usedAt:Date; createdAt:Date; updatedAt:Date; }
const schema = new Schema<LastFilterPresetDocument>({ mode:{type:String,enum:['solo','paired'],required:true,index:true}, userId:{type:Schema.Types.ObjectId,ref:'User',default:null,index:true}, pairKey:{type:String,default:null,index:true}, dishRegisters:{type:[String],default:[]}, cuisines:{type:[String],default:[]}, moods:{type:[String],default:[]}, diet:{type:[String],default:[]}, exclusions:{type:[String],default:[]}, matchedLastTime:{type:Number,default:0}, usedAt:{type:Date,default:Date.now} },{timestamps:true});
schema.index({mode:1,userId:1},{unique:true,partialFilterExpression:{mode:'solo',userId:{$exists:true,$type:'objectId'}}});
schema.index({mode:1,userId:1,pairKey:1},{unique:true,partialFilterExpression:{mode:'paired',userId:{$exists:true,$type:'objectId'},pairKey:{$exists:true,$type:'string'}}});
export const LastFilterPresetModel = model<LastFilterPresetDocument>('LastFilterPreset', schema);
